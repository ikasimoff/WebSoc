@{
    ViewBag.Title = "Home Page";
}

<div class="welcome">
    <h1>Проводите конкурсы в инстаграм</h1>
    <p class="lead">
        Быстро и без ограничений
    </p>
    <p>
        <a href="#" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#exampleModal">
            Добавить конкурс
        </a>
    </p>
</div>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Напишите страницу конкурса</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form action="/Home/GenerateConcursAsync" method="post">
                    <input name="login" />
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary" onclick="$('form').submit();">Save changes</button>
            </div>
        </div>
    </div>
</div>

@foreach(var i in ViewBag.list as IEnumerable<string>)
{
    @:<img src="@i" /> 
}